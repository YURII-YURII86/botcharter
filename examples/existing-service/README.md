# Existing service fixture

This synthetic project represents a small Python service before Telegram bot adoption.

Do not run `botctl adopt --confirm` directly in this tracked directory. Use:

```bash
python tools/demo_public_alpha.py
```

The demo operates on a temporary copy.
